# 本地视频库

本地视频库汇集用户保存目录中的视频，并保存对应的媒体信息和使用记录。

## 领域术语

**扫描合集**：当前保存的全部视频目录的并集。重复或嵌套目录中的同一文件只属于集合中的一项。
_避免_：单目录扫描结果、任意目录子集

**文件身份**：视频文件的绝对路径、文件大小与修改时间的组合。身份决定已处理的媒体信息是否仍然有效、是否需要重新处理：三者任一变化都表示身份变化，已有的媒体信息随之作废并重新处理，不同路径下内容相同的视频也具有不同身份。记录本身及其使用记录（收藏、播放历史）跟随路径，不随身份变化而消失。已接受的盲区：文件大小与修改时间都没变、只有内容被换掉（例如保留了原时间戳、长度又恰好相同的替换）不会被发现，该记录继续沿用旧的媒体信息，直到该路径上的文件下次发生大小或修改时间变化。
_避免_：仅按文件名去重、仅按内容去重、只按路径判身份、身份变化即替换记录

**增量扫描**：以扫描合集同步视频库，处理新增、文件身份变化或上次未完成的视频，身份不变且已完成的视频保留已有媒体信息。
_避免_：清空后重建

**过期视频记录**：数据库中对应文件已不属于扫描合集的记录。扫描后判定一条既有记录是否过期，看它的路径上还有没有文件：路径上已没有文件，或它所在的配置目录已不在当前保存目录中，即为过期。路径上仍有文件但本次取不到它的元数据（或它所在的子树遍历不出）时，记录不因这次扫描成为过期记录；整条配置目录存在但列不出来时，该目录整个跳过，其记录也不计入判定；整条配置目录已不存在时，其记录按该目录下的文件都已消失处理，成为过期记录。
_避免_：不可用视频（本规则下直接删除过期记录，不保留不可用占位）

**不可访问目录**：已保存、仍然存在于磁盘上，但本次扫描读不出内容的视频目录。它整条跳过，其记录不因这次扫描成为过期记录。
_避免_：目录不存在、读不出内容的单个文件

**媒体处理完成**：当前文件身份对应的媒体元数据和缩略图均已成功处理；因失败或取消而缺少任一部分时仍属未完成。
