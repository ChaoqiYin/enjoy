# 本地视频库

本地视频库汇集用户保存目录中的视频，并保存对应的媒体信息和使用记录。

## 领域术语

**扫描合集**：当前保存的全部视频目录中发现的视频文件的并集；重复或嵌套目录中的同一文件只属于集合中的一项。
_避免_：单目录扫描结果、任意目录子集

**文件身份**：视频文件的绝对路径与 MD5 的组合。路径或 MD5 任一变化，均表示身份变化；不同路径下内容相同的视频具有不同身份。
_避免_：仅按文件名去重、仅按内容去重

**增量扫描**：以扫描合集同步视频库，处理新增、文件身份变化或上次未完成的视频，身份不变且已完成的视频保留已有媒体信息。
_避免_：清空后重建

**过期视频记录**：数据库中对应文件已不属于扫描合集的记录，包括文件已消失以及其目录已不在当前保存目录中的情况。
_避免_：不可用视频（本规则下直接删除过期记录，不保留不可用占位）

**媒体处理完成**：当前文件身份对应的媒体元数据和缩略图均已成功处理；因失败或取消而缺少任一部分时仍属未完成。
