# 页面与 Rust 逻辑逐项完成审查

本表以开发基线和界面原型为范围；播放按最新决定仅调用系统默认播放器，FFmpeg 二进制打包暂缓。详细实测步骤和截图见[开发验收进度](开发验收进度.md)。本表记录可定位的证据。2026-09-11 按用户确认，页面可通过浏览器验收，系统集成使用 Rust 真实测试及已有桌面实测。

| 设计要求 | 实现与证据 | 状态 |
| --- | --- | --- |
| 多目录选择、去重和添加扫描 | 原生目录选择桌面实测；目录批次去重、失败重试及取消测试 | 已实现，批次恢复有测试 |
| 扩展名筛选、递归扫描、基础索引 | scanner、repository；仓储与真实文件通知测试 | 已验证 |
| 元数据读取和缩略图生成 | FFprobe/FFmpeg 真实媒体集成测试、桌面截图 | 已验证 |
| 增量处理、缓存复用、失效恢复 | 仓储测试、文件与目录失联恢复桌面实测 | 已验证 |
| 视频库、收藏、历史、设置 | 路由及共享状态；主要页面桌面实测 | 已验证 |
| 搜索、文件夹筛选、排序、网格与列表 | selectVideos 测试；网页 Archive 筛选显示 18 项，按名称从 02 到 36 排列；搜索和视图切换实测 | 已验证 |
| 卡片选择、双击及右键菜单 | 组件测试；右键菜单、默认播放桌面实测 | 已验证 |
| 收藏和打开历史持久化 | 仓储测试、应用重启实测 | 已验证 |
| 默认系统打开、失败不记次数 | 默认播放器真实文件窗口；实际系统打开失败映射测试、失败不记数测试及网页错误提示截图 | 已验证 |
| 文件定位和索引移除确认 | 访达选中文件；确认移除后原文件存在 | 已验证 |
| 详情抽屉及媒体信息 | 真实详情操作和媒体字段；文件大小格式修正 | 已验证 |
| 目录监听和自动刷新 | 真实通知测试、添加/移走/恢复/整体断开恢复实测 | 已验证 |
| 扫描阶段数量、暂停、继续、取消 | 桌面暂停与取消实测；真实扫描集成验证有效及损坏文件混合处理、失败不计就绪、重扫缓存复用 | 已验证 |
| 顶部固定导航和扫描百分比 | 网页滚动 800 像素后导航顶端为 0，状态保留 50%；截图 | 已验证 |
| 缩略图生成占位与进度 | 网格、列表和详情网页实测生成中提示；暂停与取消事件状态更新；组件测试覆盖路径匹配及终态清除 | 已验证 |
| 中英文即时切换及偏好持久化 | 语言测试、真实切换和重启、写入故障恢复 | 已验证 |
| 跟随系统、焦点恢复语言同步 | 系统解析与读取失败回退；新增测试覆盖系统语言同步、乱序响应、手动保存期间不读取及失败后恢复 | 同步逻辑测试通过 |
| 统一错误、翻译回退与重试 | 前端测试、真实媒体失败恢复、已有错误切换语言 | 已验证 |
| 原生菜单和启动错误翻译 | 菜单切换、数据库启动故障原生提示截图 | 已验证 |
| 长路径与窄窗口 | 中文桌面实测；500 像素英文库、设置及错误布局网页截图，文档宽度不超过视口 | 已验证 |
| 工程约束 | 源码行数、静态导入、翻译检查、构建及测试 | 本机检查通过；远端流水线未运行 |
| 媒体二进制分发 | 当前调用本机媒体工具 | 按用户要求暂缓 |

运行证据来自 macOS 和 Chromium；Windows/Linux 未实机验证，远端流水线未运行。页面与 Rust 逻辑范围已完成，FFmpeg 二进制打包仍按用户要求暂缓。


## 最终验证与复现

- 前端 21 项测试、源码约束与翻译检查、TypeScript 检查和生产构建通过。
- Rust 25 项测试（含真实 FFmpeg/FFprobe、文件通知、系统打开失败）、Clippy 和格式检查通过。
- `scripts/web-acceptance.html` 为独立网页验收入口，使用 Tauri 官方模拟通信及 36 条测试数据，不接触用户数据库，不进入生产构建入口。启动方式见开发指南。
- 2026-09-11 网页截图：`网页筛选排序.png`、`网页固定导航.png`、`网页预览生成中.png`、`网页英文窄窗口.png`、`网页英文设置窄窗口.png`、`网页打开失败.png`、`网页详情预览状态.png`，均位于 `docs/验收截图/`。
- 网页打开失败来自注入的业务错误；实际系统错误映射由 `player::tests::system_open_failure_returns_localizable_error` 验证，失败不记次数由仓储回归测试验证。网页模拟不宣称验证了播放器、原生菜单或文件系统。
