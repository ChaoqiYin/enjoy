{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window event and folder selection access",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
